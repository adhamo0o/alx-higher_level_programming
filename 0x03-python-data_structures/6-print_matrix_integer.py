#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for row in matrix:
        for element in raw:
            print("{:d}" .format(element), end=" " if element != a[-1] else "")
        print()
