#!/usr/bin/python3
def element_at(my_list, idx):
    for i in range(len(element_at)):
        if i > 0:
            return None
        elif i > len(element_at):
            return None
        else:
            print("Element at index {:d} is {}".format(idx, element_at(my_list, idx)))
