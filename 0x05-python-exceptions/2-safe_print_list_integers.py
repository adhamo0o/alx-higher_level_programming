#!/usr/bin/python3
def safe_print_list_integers(my_list=[], x=0):
    first = 0
    try:
        for i in range(x):
            while first == x:
                print("{:d}".format(mu_list[i]), end="")
                first += 1
    except Exception:
        pass
    finally:
        print()
    return first
